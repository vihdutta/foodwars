{
  "compilerOptions": {
    "target": "es2017", // Target ECMAScript version
    "module": "ESNext", // Use ESNext module system
    "strict": false, // Enable strict type-checking
    "esModuleInterop": true, // Allow default imports from CommonJS modules
    "moduleResolution": "node", // Resolve modules in Node.js style
    "skipLibCheck": true, // Skip type checking of declaration files
    "resolveJsonModule": true, // Added to potentially help with JSON imports if any
    "sourceMap": true // Added to generate source maps for debugging
  },
  "type": "module",
  "include": ["src/**/*", "public/js/**/*.ts"], // Include both backend and frontend TS files
  "exclude": ["node_modules", "src/tilesets"] // Exclude node_modules and tilesets
}
